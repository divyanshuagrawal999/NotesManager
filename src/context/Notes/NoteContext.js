import { createContext } from "react";

const NoteContext = createContext();

export default NoteContext;

//here we are just creating context api to use them and exporting them to use in other files